{% extends 'back/base.html.twig' %}

{% block body %}
    <h1>Quiz edit</h1>

    {{ form_start(edit_form) }}
    <div class="form-group">
        {{ form_label(edit_form.quizName) }}
        {{ form_errors(edit_form.quizName) }}
        {{ form_widget(edit_form.quizName) }}
    </div>
    <div class="form-group">
        {{ form_label(edit_form.quizCategory) }}
        {{ form_errors(edit_form.quizCategory) }}
        {{ form_widget(edit_form.quizCategory) }}
    </div>
    <div class="form-group">
        {{ form_label(edit_form.status) }}
        {{ form_errors(edit_form.status) }}
        {{ form_widget(edit_form.status) }}
    </div>
    {% include 'back/common/edit_button.html.twig' %}
    {{ form_end(edit_form) }}


    <ul class="buttons">
        <li>
            <a href="{{ path('quiz_index') }}" class="btn btn-primary fixed_left back-to-list-button">
                <i class="fa fa-list"></i>
                Back to the list
            </a>
        </li>
        <li >
            {% block delete_button %}
                {% include 'back/common/delete_button.html.twig' %}
            {% endblock %}
        </li>
    </ul>
{% endblock %}
